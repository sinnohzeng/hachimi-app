/// AppElevation — Material Design 3 elevation tokens.
///
/// M3 Elevation Scale:
///   level0 (0dp) — 背景面
///   level1 (1dp) — Card 默认
///   level2 (3dp) — NavigationBar、FAB lowered
///   level3 (6dp) — FAB、SnackBar
///   level4 (8dp) — NavigationDrawer
///   level5 (12dp) — Modal
class AppElevation {
  AppElevation._();

  static const double level0 = 0;
  static const double level1 = 1;
  static const double level2 = 3;
  static const double level3 = 6;
  static const double level4 = 8;
  static const double level5 = 12;
}
